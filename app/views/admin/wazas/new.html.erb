<div class="container py-4">
  <div class="row justify-content-center">
    <div class="col-12 col-md-8 col-lg-6">
      <div class="card shadow-sm">
        <div class="card-body">
          <h1 class="h4 mb-3">技 <%= action_name == "new" ? "新規作成" : "編集" %></h1>
          <%= render "form" %>
          <div class="mt-2"><%= link_to "一覧へ戻る", admin_wazas_path %></div>
        </div>
      </div>
    </div>
  </div>
</div>
